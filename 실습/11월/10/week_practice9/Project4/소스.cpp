#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <stdlib.h>)

int main() {

	char buffer[256] = { 0 };
	int a;
	int b;
	int c;
	int d;
	int e;
	int f;

	
	scanf("%s", buffer);
	sscanf(buffer, "[%d, %d, %d]+[%d,%d,%d]", &a, &b, &c);

	//µîµî
}